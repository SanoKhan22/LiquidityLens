# Application Configuration
spring.application.name=LiquidityLens Backend

# Server Configuration
server.port=8080

# CRITICAL: Enable Virtual Threads (Project Loom)
spring.threads.virtual.enabled=true

# Binance WebSocket Configuration
binance.websocket.url=wss://stream.binance.com:9443/ws/btcusdt@depth20@100ms

# Logging Configuration
logging.level.root=INFO
logging.level.com.liquiditylens=DEBUG

# WebSocket Configuration
spring.websocket.mapping=/market-feed
